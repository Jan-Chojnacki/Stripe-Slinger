fmt:
  stage: lint
  script:
    - cargo +nightly fmt --all -- --check

clippy:
  stage: lint
  needs: ["fmt"]
  script:
    - cargo +nightly clippy --workspace --all-targets --all-features -- -D warnings


